#!/bin/bash

green='\033[0;32m'
end='\033[0m'


echo "${green}Running pre-commit linting${end}"

committedFiles=$(git diff --name-only --cached)
files=$(echo $committedFiles | tr ";" "\\n")

# make lint

echo "${green}Fixed all issues automatically. Committing..! :)${end}"

echo $files | touch "files_for_commit.txt"

# for file in $files
# do
#     git add $file
# done
# git commit --amend --no-edit
# exit 0


