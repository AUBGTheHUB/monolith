import { SectionTitle } from '../shared/SectionTitle';

export const ScheduleHeader = () => (
    <SectionTitle title="SCHEDULE" iconSrc="/ScheduleSection/logo.png" iconAlt="HackAUBG Logo" dark={false} />
);
