import { RegisterForm } from './RegisterForm';

export function RegisterPage() {
    return (
        <main className="min-h-screen w-full flex items-center justify-center p-8 bg-[#000912]">
            <RegisterForm />
        </main>
    );
}
