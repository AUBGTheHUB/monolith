import { LoginForm } from './LoginForm';

export function LoginPage() {
    return (
        <main className="min-h-screen w-full flex items-center justify-center p-4 bg-[#000912]">
            <LoginForm />
        </main>
    );
}
