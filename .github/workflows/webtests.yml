name: Build and Test Frontend

on:
    pull_request:
        types: [assigned, opened, synchronize, reopened]
    workflow_dispatch:

jobs:
    build_and_test_frontend:
        runs-on: ubuntu-latest

        steps:
            # Check out the repository
            - uses: actions/checkout@v4

            # Debug the directory structure (optional but useful for troubleshooting)
            - name: Show current working directory
              run: pwd

            - name: Debug repository structure
              run: ls -R

            # Set up Node.js environment
            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '16'

            # Install dependencies
            - name: Show current working directory before installing dependencies
              run: pwd

            - name: Install dependencies
              run: npm install

            # Build the frontend
            - name: Show current working directory before building
              run: pwd

            - name: Build the frontend
              run: npm run build

            # Run frontend tests
            - name: Show current working directory before running tests
              run: pwd

            - name: Run frontend tests
              run: npm test
